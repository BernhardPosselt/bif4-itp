// Generated by CoffeeScript 1.3.3
(function() {

  window.WebChat || (window.WebChat = {});

  window.WebChat.WebSocket = (function() {

    function _Class() {
      this._callbacks = {
        onOpen: function() {},
        onReceive: function() {},
        onError: function() {
          return console.error("websocket error occured");
        },
        onClose: function() {
          return console.info("closed websocket");
        }
      };
    }

    _Class.prototype.connect = function(domain, path, ssl) {
      var Socket, protocol, url,
        _this = this;
      if (domain == null) {
        domain = document.location.host;
      }
      if (path == null) {
        path = "/websocket";
      }
      this.ssl = ssl != null ? ssl : false;
      Socket = window['MozWebSocket'] || window['WebSocket'];
      if (this.ssl) {
        protocol = "wss://";
      } else {
        protocol = "ws://";
      }
      url = "" + protocol + domain + path;
      try {
        this._connection = new Socket(url);
        this._connection.onopen = function() {
          _this._callbacks.onOpen();
          return window.onbeforeunload = function() {
            return _this.close();
          };
        };
        this._connection.onmessage = function(event) {
          var json, msg;
          msg = event.data;
          json = JSON.parse(msg);
          _this._callbacks.onReceive(json);
          return console.info("Received: " + msg);
        };
        this._connection.onclose = function() {
          return _this._callbacks.onClose();
        };
        return this._connection.onerror = function() {
          return _this._callbacks.onError();
        };
      } catch (error) {
        return console.error("Cant connect to " + url);
      }
    };

    _Class.prototype.onOpen = function(callback) {
      return this._callbacks.onOpen = callback;
    };

    _Class.prototype.onReceive = function(callback) {
      return this._callbacks.onReceive = callback;
    };

    _Class.prototype.onClose = function(callback) {
      return this._callbacks.onClose = callback;
    };

    _Class.prototype.onError = function(callback) {
      return this._callbacks.onError = callback;
    };

    _Class.prototype.send = function(msg) {
      this._connection.send(msg);
      return console.info("Sending " + msg);
    };

    _Class.prototype.sendJSON = function(json_object) {
      var msg;
      msg = JSON.stringify(json_object);
      return this.send(msg);
    };

    return _Class;

  })();

}).call(this);
