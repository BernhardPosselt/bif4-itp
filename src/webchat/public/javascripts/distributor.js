// Generated by CoffeeScript 1.3.3

/*
Gets the messages from the websocket and forwards it to the corresponding 
controllers
*/


(function() {

  window.WebChat || (window.WebChat = {});

  window.WebChat.Distributor = (function() {

    function _Class() {
      this.controllers = [];
    }

    _Class.prototype.setCache = function(cache) {
      this.cache = cache;
    };

    _Class.prototype.registerController = function(controller) {
      return this.controllers.push(angular.element(controller));
    };

    _Class.prototype.notifyController = function(message) {
      var controller, controllerElement, scope, _i, _len, _ref, _results;
      message = this.cache.fill(message);
      _ref = this.controllers;
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        controllerElement = _ref[_i];
        scope = controllerElement.scope();
        controller = controllerElement.controller();
        if (controller.canHandle(message.type)) {
          _results.push(scope.$apply(function() {
            switch (message.action) {
              case 'update':
                return controller.update(message.data);
              case 'create':
                return controller.create(message.data);
              case 'delete':
                return controller["delete"](message.data);
            }
          }));
        } else {
          _results.push(void 0);
        }
      }
      return _results;
    };

    return _Class;

  })();

}).call(this);
