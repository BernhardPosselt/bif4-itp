// Generated by CoffeeScript 1.3.3
(function() {

  window.WebChat || (window.WebChat = {});

  window.WebChat.BaseController = (function() {

    function _Class($scope, type) {
      this.$scope = $scope;
      this.type = type;
      $scope.create = function(item) {
        return $scope.items.push(item);
      };
      $scope.update = function(updatedItem) {
        var counter, item, _i, _len, _ref, _results;
        _ref = $scope.items;
        _results = [];
        for (counter = _i = 0, _len = _ref.length; _i < _len; counter = ++_i) {
          item = _ref[counter];
          if (item.id === updatedItem.id) {
            _results.push($scope.items[counter] = updatedItem);
          } else {
            _results.push(void 0);
          }
        }
        return _results;
      };
      $scope["delete"] = function(removedItem) {
        var counter, item, removeItemId, _i, _len, _ref;
        removeItemId = -1;
        _ref = $scope.items;
        for (counter = _i = 0, _len = _ref.length; _i < _len; counter = ++_i) {
          item = _ref[counter];
          if (item.id === removedItem.id) {
            removeItemId = counter;
          }
        }
        if (removeItemId >= 0) {
          return $scope.items.splice(removeItemId, 1);
        }
      };
    }

    _Class.prototype.canHandle = function(messageType) {
      return messageType === this.type;
    };

    return _Class;

  })();

}).call(this);
